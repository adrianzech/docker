services:
  isponsorblocktv:
    container_name: isponsorblocktv
    image: ghcr.io/dmunozv04/isponsorblocktv
    restart: unless-stopped
    network_mode: host
    volumes:
    - /opt/appdata/isponsorblocktv:/app/data
